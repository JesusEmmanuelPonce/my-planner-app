import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    text: {
        color: '#fff',
        fontSize: 30,
        fontWeight: 'bold',
        paddingTop: 25,
        textAlign: 'center',
        textTransform: 'uppercase',
    }
})